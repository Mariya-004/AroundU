steps:
  # Step 1: Install dependencies
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']
    dir: 'backend/functions/deliveryagent_accept_reject'

  # Step 2: Deploy the function
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    args:
      - 'gcloud'
      - 'functions'
      - 'deploy'
      - 'deliveryagent_accept_reject'
      - '--gen2'
      - '--runtime=nodejs22'
      - '--region=asia-south1'
      - '--source=./backend/functions/deliveryagent_accept_reject'
      - '--entry-point=deliveryagent_orderstatus' # Corrected this to match your likely export
      - '--trigger-http'
      - '--allow-unauthenticated'
      # Corrected way to set environment variables for clarity and safety
      - '--set-env-vars'
      - 'MONGO_URI=$_MONGO_URI,JWT_SECRET=$_JWT_SECRET'

# This block must be at the root level (no indentation)
options:
  # This line must be indented with two spaces
  logging: CLOUD_LOGGING_ONLY